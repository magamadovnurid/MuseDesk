# Проверки выпуска 1.22.0 preview

## Дополнение: macOS и совместный выпуск 1.23

7 сентября 2026 года [прогон на Apple Silicon](https://github.com/magamadovnurid/MuseDesk/actions/runs/34151752139) успешно выполнил проверки политики памяти, файлов, истории, изоляции модели и интерфейса. Реальный архив Ollama 0.33.3 Darwin скачан, проверен по SHA-256, распакован и запущен; API подтвердил версию и пустой список моделей. Созданы DMG и ZIP, проверены Mach-O arm64, `codesign --verify --deep --strict` и запуск упакованного `.app` с тестовыми данными.

Mac-скриншоты в документации получены из этого CI-прогона. Последующие изменения дополнительно проверяют отмену shell-команды и блокировку ввода на этапе загрузки. Публикация совместного выпуска требует успешного CI для финального тега на обеих платформах.

Загрузка полных HF-весов и генерация через Metal на физических Mac с 32/36/48/64+ ГБ пока не подтверждены. Проверка кода и запуска движка не заменяет этот аппаратный этап. Ad-hoc подпись не является Developer ID и не подтверждает прохождение Gatekeeper на компьютере получателя.

## Исходный Windows-прогон

Дата: 7 сентября 2026 года. Проверки выполнялись на Windows разработчика, в изолированных папках. Работающий установленный пакет не обновлялся.

| Проверка | Результат |
|---|---|
| `scripts/test-windows.ps1` | ALL CHECKS PASSED; макеты API, проекты, диалоги, готовность модели, UI и снимки окна |
| `scripts/build-installer.ps1` | Отдельный x64 EXE собран с приложением, каталогом и backend внутри |
| `scripts/test-installer.ps1` | Пройдены профили оборудования, SHA-256 метаданных, отмена, кэш, отказ при повреждении, zip-slip и app-only установка |
| Настоящее скачивание Ollama 0.33.3 | Скачаны 1 469 175 900 байт; SHA-256 совпал с каталогом; архив распакован |
| Настоящее скачивание весов с закреплённого HF URL | Началось успешно; после прерывания около 138 МБ продолжилось до 181 МБ без повторной загрузки с нуля |
| Штатная отмена при скачивании | Дочерний downloader остановлен, backend завершился, `.part` сохранён |
| Скриншоты | Настоящие формы приложения и установщика; демонстрационные данные, без личных чатов |

## Что ещё не подтверждено

- Полное скачивание всех весов и их конечная контрольная сумма в end-to-end установке. Закреплённые хеши взяты из метаданных издателя; тест загрузки намеренно остановлен.
- Чтение нового манифеста движком с этими полными HF-весами и успешная генерация текста/изображений.
- Чистая Windows другого пользователя, различные драйверы, GPU 24/32+ ГБ, масштабирование 125–200% и медленные/ограниченные сети.
- Причина прежних аппаратных перезагрузок. Проверка установщика не устанавливает причину hard reset и не доказывает исправность питания, драйвера или GPU.

В этой проверке установщик не запускал Ollama, загрузку модели в видеопамять или inference. Следующий приёмочный этап — чистая установка, подтверждение единственной резидентной модели и короткий ответ с сохранением журналов на отдельно выбранной тестовой машине.

## Выпуск 1.24.0-preview.1 — 7 сентября 2026

[Релизная проверка 34152952828](https://github.com/magamadovnurid/MuseDesk/actions/runs/34152952828) завершилась успешно: Windows, Apple Silicon, аудит приватности и публикация всех четырёх пакетов.

Локально: 450 успешных проверок Windows, 22 проверки установщика; 15 тестов Mac пройдены и один платформенный тест пропущен на Windows. На Mac CI выполнены все 16 тестов, проверка интерфейса, запуск настоящего движка без моделей, подпись/архитектура пакета и запуск собранного приложения.

Официальный пакет .NET 4.8.1 скачан и проверен Authenticode (Microsoft Corporation), но не установлен на рабочую машину. Проверка зависимостей pnpm audit: 271 пакет, известных уязвимостей не обнаружено. Gitleaks: 15 коммитов, секретов не обнаружено; проверены также персональные метаданные и 148 исторических путей.

Опубликованный Windows Setup повторно скачан с GitHub, его SHA-256 совпал с метаданными релиза. Копия помещена в отдельную папку Muse Desk Install. Существующий установленный MuseDesk 1.21.5 сохранён.


## Выпуск 1.24.1-preview.1 — 8 сентября 2026

[Проверка 34215014590](https://github.com/magamadovnurid/MuseDesk/actions/runs/34215014590) успешно завершила Windows, macOS, аудит приватности и публикацию. Локально прошли 456 проверок Windows и 22 проверки установщика. Сценарии зацикливания и уточнений воспроизведены синтетическими ответами модели, без реальной генерации на рабочей GPU. На Mac выполнены тесты и проверка собранного приложения. Подробности: [поведение агента](AGENT-BEHAVIOR.md).

Опубликованный Windows-установщик скачан повторно; SHA-256 совпал с метаданными GitHub. Установленный локальный EXE обновлён до 1.24.1 с сохранением прежнего файла и истории; уже открытый процесс использует старую версию до перезапуска.


## Выпуск 1.24.2-preview.1 — 8 сентября 2026

Локально прошли 469 проверок Windows и 22 проверки установщика. Новые сценарии проверяют общий скролл длинного итога и кода, колесо мыши над ответом, метки вопросов, подсказки, переходы мышью и Alt+стрелками, видимость последней строки и сохранение положения при поступлении ответа. Проверки используют синтетический текст, без запуска моделей.

Для Mac локально пройдены 15 тестов (один платформенный тест пропущен на Windows) и Electron UI smoke: метки, настоящий hover в тестовом окне, границы подсказки, плавный переход к вопросу, отсутствие внутреннего скролла ответа и компактная форма ввода. [Релизный CI 34217605271](https://github.com/magamadovnurid/MuseDesk/actions/runs/34217605271) успешно проверил Windows, Apple Silicon, публичный репозиторий и опубликовал EXE, DMG и оба ZIP. Дополнительная локальная проверка подтверждает, что полоса навигации находится поверх области переписки.

Windows-установщик повторно скачан из выпуска 1.24.2, SHA-256 совпал с метаданными GitHub; копия обновлена в отдельной папке Muse Desk Install. Локальный EXE обновлён до 1.24.2 с резервной копией предыдущего файла. Уже открытое окно получает изменения после полного перезапуска приложения.

## Выпуск 1.24.3-preview.1 — 8 сентября 2026

[Релизный CI 34218576186](https://github.com/magamadovnurid/MuseDesk/actions/runs/34218576186) успешно завершил Windows, Apple Silicon, аудит приватности и публикацию EXE, DMG и ZIP. Локально прошли 473 проверки Windows, 22 проверки установщика и тесты Mac (15 локально, один платформенный тест — в Mac CI).

Центральная лента проверена на отсутствие рамки итога и повторного текста, порядок действий перед ответом, статус «Готово», отсутствие успешного статуса при отмене, копирование полного ответа с файлами, сохранение раскрытия действий в Mac, общую прокрутку, потоковое отображение и даты сообщений. Снимки сделаны на синтетических диалогах; GPU-генерация на рабочей машине не запускалась.

Windows EXE обновлён локально с резервной копией предыдущей версии. Опубликованный установщик повторно скачан, его SHA-256 совпал с метаданными GitHub; обновлена отдельная папка Muse Desk Install.

## Выпуск 1.24.4-preview.1 — 8 сентября 2026

[Релизный CI 34219341142](https://github.com/magamadovnurid/MuseDesk/actions/runs/34219341142) успешно проверил Windows, Apple Silicon и приватность репозитория; опубликованы EXE, DMG и ZIP. Mac: 22 теста, включая чтение физической памяти на реальной macOS. Локально на Windows прошли 20 тестов Mac, два платформенных теста выполнены в Mac CI.

Проверены запрет любых загрузок при попытке установить Glimmer на Mac с 8/16/24 ГБ, неизвестная память, неподходящая ОС/архитектура, нехватка диска, недоверие к сохранённому профилю и повторное сканирование перед весами. Проверен отказ до запуска движка при попытке загрузить ранее установленную Glimmer на Mac с 16 ГБ. Положительные профили 32/64 ГБ используют синтетические скачивания; это не аппаратная проверка полной модели. UI smoke подтверждает сообщение об отказе и недоступную кнопку установки. Опубликованный Windows Setup повторно скачан и проверен по SHA-256.

Точный путь, который вызвал сообщение пользователя о скачивании на неподходящем Mac в прежнем выпуске, не воспроизведён: параметры устройства, версия приложения и прежний журнал пока не получены. Старый мастер разрешал отдельно скачать движок при недостаточной памяти; новый мастер этого не предлагает. Дополнительно устранен допуск профиля при неизвестных числовых показаниях.

## Выпуск 1.24.5-preview.1 — 8 сентября 2026

[Релизный CI 34220237225](https://github.com/magamadovnurid/MuseDesk/actions/runs/34220237225) успешно проверил Windows, Apple Silicon и приватность репозитория; опубликованы EXE, DMG и ZIP. Локально прошли 476 проверок Windows и 22 проверки установщика. Mac: 22 теста в CI и UI smoke собранного приложения.

Проверены положение навигатора слева по центру высоты переписки, наличие единственной отдельной полосы прокрутки справа, видимость штрихов в коротком чате, подсказки, переходы к вопросам и отметка текущей темы. Интерактивная проверка Electron использует движение указателя в тестовом окне; Windows проверяет переходы мышью и клавиатурой. GPU-генерация не запускалась.

Локальный Windows EXE обновлён до 1.24.5 с резервной копией предыдущей версии. Опубликованный установщик повторно скачан и проверен по SHA-256; обновлена отдельная папка Muse Desk Install. Новая компоновка появляется после полного перезапуска приложения.

## Выпуск 1.24.6-preview.1 — 8 сентября 2026

[Релизный CI 34220958612](https://github.com/magamadovnurid/MuseDesk/actions/runs/34220958612) успешно проверил Windows, Apple Silicon и приватность; опубликованы EXE, DMG, ZIP и контрольные суммы. Локально прошли 476 проверок Windows, 22 проверки установщика и 20 тестов Mac (два платформенных теста выполнены в Mac CI).

Увеличены подписи нижней строки чата, уменьшена круглая кнопка отправки. Проверены компоновка Windows при ширине 940 пикселей и снимки интерфейса Electron на синтетическом диалоге. Генерация на GPU не запускалась. Локальный Windows EXE обновлён с резервной копией; официальный установщик проверен по SHA-256 и скопирован в отдельную папку Muse Desk Install.

## Выпуск 1.25.0-preview.1 — 8 сентября 2026

[Релизный CI 34242359481](https://github.com/magamadovnurid/MuseDesk/actions/runs/34242359481) успешно проверил Windows, macOS Apple Silicon, Ubuntu и приватность репозитория. Опубликованы Windows EXE/ZIP, macOS DMG/ZIP, Ubuntu amd64 DEB и общий SHA256SUMS.txt.

[Матрица Ubuntu 34242273576](https://github.com/magamadovnurid/MuseDesk/actions/runs/34242273576) прошла на Ubuntu 22.04 и 24.04: unit-тесты, скачивание Linux Ollama 0.33.3 с проверкой SHA-256, запуск настоящего движка с пустым списком моделей, сборка DEB, установка через APT и UI smoke установленного приложения. На каждой Ubuntu прошли 26 тестов, один Mac-специфичный тест пропущен. Локально на Windows прошли 24 общих теста, три платформенных теста пропущены.

Проверены отказ от скачивания при недостаточной RAM/VRAM или неподходящем драйвере, выбор одной GPU без суммирования памяти карт, Linux-метаданные модели, Bash и отмена команд, общий интерфейс, кнопки окна, проекты, навигатор, блокировка ввода до готовности и компоновка при ширине 940 пикселей. Скриншоты Ubuntu в документации сняты с установленного DEB в CI на синтетических диалогах.

Опубликованный DEB повторно скачан; SHA-256 совпал с digest GitHub и общим файлом контрольных сумм. Полная генерация Glimmer на физической NVIDIA в Ubuntu не проверялась. Критерии оборудования являются допуском к установке, а не подтверждением аппаратной приёмки каждой конфигурации.
